from setuptools import setup, find_packages

setup(
    name='pyze',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    tests_require=[''],
    author='Zero Enter',
    description='Zero Enter sdk',
)